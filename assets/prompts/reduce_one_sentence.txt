你正在帮 UX 团队聚合多位受访者的画像数据。

## 当前任务

字段 `one_sentence_need`(一句话需求)的合并。共有 {{TOTAL}} 位受访者参与访谈。

下方是各位的一句话需求:

{{INPUTS}}

## 你要做的

从所有一句话需求里**选出最能代表整体共识**的一条:

1. 选取标准:能代表多数共性诉求 + 表达精炼 + 信息密度高
2. **只挑选,不改写**:输出的整句必须能在该受访者的原始文本里一字不差搜到
3. 保留原 `[来源:文件名]` 标注

## 输出格式

只输出单个 JSON 字符串,输出内容仅含 JSON,不带 markdown 标题、解释或前后语:

```json
"[来源:访谈_邓老师.txt]: \"我们就希望系统能更聪明,别让我们当人肉过滤器\""
```

注意输出是单个字符串,不是数组,也不是对象。

## 关键约束

- 引号内 ≤ 60 字
- 必须保留 `[来源:xxx]: "..."` 的标注格式
- 输出原话必须能在该受访者的原文里一字不差搜到

## 选取反例

**错误:为追求"概括"自己拼一句**

输入:邓老师说"希望系统能更聪明",孔老师说"别让我们当人肉过滤器",刘老师说"少误报多准确"

错误输出:"希望系统聪明、减少误报、不当人肉过滤器"(拼凑出来的,任何一位都没说过)

正确输出:从三人的原话里挑信息密度最高的一条,例如选邓老师的「我们就希望系统能更聪明,别让我们当人肉过滤器」(已经把多个共识合在一句里),保留原标注

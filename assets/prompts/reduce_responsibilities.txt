你正在帮 UX 团队聚合多位受访者的画像数据。

## 当前任务

字段 `responsibilities`(工作职责及占比)的语义聚类。共有 {{TOTAL}} 位受访者参与访谈。

下方是部分受访者各自提到的职责及占比:

{{INPUTS}}

## 你要做的

合并出全部与研究问题相关、语义独立且有证据的职责:

1. **语义聚类**:不同受访者描述同一职责的合并(如「实时监盘」「监盘与潮流监控」「电网监控」)
2. **占比边界**:材料明确给出占比时才能计算。跨受访者平均属于 `synthesis`，必须写明计算依据；缺少比例时写 `null`
3. **完整保存**:保留全部独立职责。视觉层需要三项圆环时，由 04 `field_decisions` 提出压缩方案并让用户确认
4. **统计提及**:每条标明哪些受访者提过该职责

## 输出格式

只输出 JSON 数组,**不要前言后记**:

```json
[
  {
    "task": "职责描述(规范化措辞,≤ 25 字)",
    "percentage": null,
    "percentage_provenance": "not_mentioned",
    "calculation_basis": "材料未明确给出工作占比",
    "mention_count": 4,
    "mentioned_by": ["P11111111", "P22222222"],
    "evidence_quotes": [
      "[来源:P11111111]: \"原话\"",
      "[来源:P22222222]: \"原话\""
    ]
  }
]
```

## 关键约束

- 输出全部有证据的独立职责
- `percentage` 是 0-100 的数字或 `null`；只在全部比例有明确材料或已标记的研究计算时要求合计 = 100
- `percentage_provenance` 只能是 `direct`、`synthesis`、`not_mentioned`
- 优先按提及人数排序；有可靠比例时再按比例排序
- task 描述要规范化(去掉个人化措辞,采用通用职责名)
- mention_count = mentioned_by 长度 = evidence_quotes 长度,三者一致
- **evidence_quotes 必须全量**:mentioned_by 里每一位都要给出对应原话
- 优先选择多人共同提到的职责

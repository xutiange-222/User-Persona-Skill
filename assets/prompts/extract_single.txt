你正在帮助 UX 团队从访谈逐字稿提取可追溯的用户画像数据。

## 任务

从下方的访谈逐字稿中提取一个 `{{PERSONA_TYPE}}` 用户画像的结构化信息，以 JSON 格式输出。

## 要提取的字段

**只输出以下字段集中列出的字段**(包括用户在字段对齐阶段添加的自定义字段),字段集之外的字段不出现在 JSON 里:

{{FIELDS}}

字段定义和填写规范见本 prompt 的“字段说明”。调用方也可能在字段清单中附加更具体的定义；具体定义优先。

## 关键约束

### 1. 证据原则
**每个观点都要从原文提取证据**。证据必须逐字来自访谈原文。

代表性原话格式:
```
[来源:{{SOURCE_FILE}}]: "原话片段(20-50 字)"
```

### 2. 事实字段禁止推断
- 访谈没有明确提到相关内容 → 字段值填字符串 `"材料未提及"`
- 禁止用常识、岗位印象、相邻受访者或补充材料猜测该受访者的事实。
- 研究者综合只能在后续独立的 `synthesis` 层表达，不能混入单访谈事实字段。

### 3. 数字字段必须是数字
`responsibilities` 的 `percentage` 仅在原文明确给出占比时使用 0 到 100 的 JSON number；未给出时写 `null`。禁止根据语气、篇幅或常识编造占比。

### 4. 列表字段完整性
- `responsibilities`、`pain_points`、`scenarios`、`experience_goals`、`main_tasks`、`kpi`、系统和工具列表：提取全部与研究问题相关、语义独立且有直接证据的项目，不按页面容量截断。
- 重复表达只合并同义项；不同场景、条件、角色或结果分别保留。
- `representative_quotes` 是精选字段，保留 2-4 条最能代表当前受访者差异的逐字原话。

### 5. 文本长度约束(为渲染布局服务)

**长度是软上限,信息密度优先**。当原话信息丰富、压缩到字数上限会损失关键信息时,允许略微超出,优先保住"读者能 0 秒看懂"。不要为凑字数硬缩成"降噪""整合"这种无信息词,而真实诉求是"减少误报、提高准确率"时,标题该写完整就写完整。

| 字段 | 长度限制 |
|------|---------|
| `name` | ≤ 16 字 |
| `description` | ≤ 40 字 |
| `basic_profile.background` | ≤ 50 字,≤ 2 句 |
| `basic_profile` 各短字段(gender/role/department 等) | ≤ 12 字 |
| `knowledge_background.domain` | ≤ 30 字 |
| `knowledge_background.tools` | ≤ 30 字 |
| `knowledge_background.experience` | ≤ 30 字 |
| `responsibilities[].task` | ≤ 25 字 |
| `collaboration.demand_source` | ≤ 30 字 |
| `collaboration.deliverables` | ≤ 30 字 |
| `collaboration.downstream_flow` | ≤ 40 字 |
| `collaboration.kpi` | ≤ 40 字 |
| `scenarios[].scenario` | ≤ 16 字 |
| `pain_points[].title` | 通常 4-12 字；语义完整、一行可读,技术 token 不截断 |
| `pain_points[].detail` | ≤ 50 字 |
| `experience_goals[].title` | 通常 4-12 字；语义完整、一行可读,技术 token 不截断 |
| `experience_goals[].detail` | ≤ 25 字 |
| `main_tasks[]` | 每条 ≤ 30 字，条数由材料决定 |
| `high_freq_tasks[].task` | ≤ 20 字 |
| `high_freq_tasks[].frequency` | ≤ 12 字 |
| `high_freq_tasks[].process` | ≤ 40 字 |
| `kpi[]` | 每条 ≤ 35 字，条数由材料决定 |
| `business_systems[].system` | ≤ 16 字 |
| `business_systems[].scenario` | ≤ 25 字 |
| `ai_assist_systems[].system` | ≤ 16 字 |
| `ai_assist_systems[].scenario` | ≤ 25 字 |
| `fault_scenarios[].scenario` | ≤ 20 字 |
| `fault_scenarios[].handling` | ≤ 40 字 |
| `one_sentence_need` | 引号内 ≤ 60 字 |
| `representative_quotes[]` | 引号内 ≤ 50 字 |

**超长时优先压缩"目前 / 主要 / 大概"这类填充词,保留信息密度**。如果原话超长截取片段时,选最有信息量的核心句,不要包含寒暄或铺垫。

### 6. 输出格式
**输出内容仅含 JSON**，不带代码围栏、Markdown 标题、解释或前后语。

### 7. 字段不混杂维度(常见错误)

同一字段只放一个维度的信息,**不要把业务系统名写进 tools,也不要把领域知识写进 tools**。

错误示例:

```json
"knowledge_background": {
  "domain": "调度员",
  "tools": "南瑞 SCADA、Open3000、D5200、AGC、AVC、OMS,熟悉电网潮流分析"
}
```

(把业务系统名 + 领域知识全塞进 tools)

正确示例:

```json
"knowledge_background": {
  "domain": "电力系统运行规程、电网潮流分析",
  "tools": "南瑞系调度自动化、AGC/AVC 控制类系统、OMS 工单类(熟练)",
  "experience": "8 年省调经验"
}
```

- `domain` 放业务领域知识(规程、流程、原理)
- `tools` 放工具/平台能力,按**类别概括**呈现而非详细罗列(详细列名留给 `business_systems`)
- 如果同时启用了 `tools` 和 `business_systems`,前者概括分类,后者列具体名字

## 字段说明(逐字段)

### 用户身份类
- `basic_profile`(dict):性别、部门、岗位、工作年限、职业经历(background)
- `knowledge_background`(dict):领域知识(domain)、工具能力(tools)、经验积累(experience)

### 工作内容类
- `responsibilities`(list of {task, percentage}):全部有证据的独立职责；原文未给占比时 percentage 为 null
- `main_tasks`(list of string):全部有证据且与研究问题相关的主要业务任务(比 responsibilities 更细的实操任务)
- `high_freq_tasks`(list of {task, frequency, process}):每天/每周必做的任务,任务名 + 频次 + 处理流程概述
- `kpi`(list of string):岗位考核指标,具体指标 + 度量方式
- `collaboration`(dict):需求来源(demand_source)、交付物(deliverables)、流转去向(downstream_flow)
- `business_systems`(list of {system, scenario}):使用的业务系统名 + 在哪个场景用
- `ai_assist_systems`(list of {system, scenario}):使用的 AI 辅助工具名 + 在哪个场景用
- `fault_scenarios`(list of {scenario, handling}):典型故障/异常场景 + 处理方式
- `scenarios`(list of {scenario, tools}):通用业务场景(如果分了上面更细的 business_systems / fault_scenarios 等,通常不再用 scenarios)

### 用户感受类
- `pain_points`(list of {title, detail}):完整保留有证据的独立痛点,带语义完整的一行 title + detail(具体可观察)
- `experience_goals`(list of {title, detail}):完整保留有证据的独立目标,同上结构

### 用户原声类
- `one_sentence_need`(string):带 [来源:匿名 source ID] 前缀的一句话
- `representative_quotes`(list of string):2-4 条带 [来源:] 前缀的原话

## 输出 JSON 结构示例(包含部分新字段)

```json
{
  "name": "电力调度员",
  "description": "负责省级电网日常调度,长期被多系统切换和异常预警准确性问题困扰",
  "user_count": 1,
  "persona_type": "{{PERSONA_TYPE}}",
  "source_documents": ["{{SOURCE_FILE}}"],

  "basic_profile": {
    "department": "电网调度中心",
    "role": "高级调度员",
    "work_years": "5-10年",
    "background": "电力专业本科,在调度中心工作 8 年,负责日常调度和应急处理"
  },

  "knowledge_background": {
    "domain": "电力系统运行、调度规程",
    "tools": "EMS 调度自动化系统、SCADA、DTS",
    "experience": "8 年省级电网调度经验"
  },

  "responsibilities": [
    {"task": "实时监盘与潮流调整", "percentage": null},
    {"task": "异常处理与故障应急", "percentage": null},
    {"task": "操作票审核与执行", "percentage": null}
  ],

  "main_tasks": [
    "根据负荷曲线调整发电出力",
    "审核操作票并下达调度指令",
    "处理跳闸、过载等异常告警",
    "应对极端天气下的负荷波动"
  ],

  "high_freq_tasks": [
    {"task": "实时监盘", "frequency": "全天持续", "process": "看 SCADA 主图 + 多套系统轮巡"},
    {"task": "操作票审核", "frequency": "每天 5-10 张", "process": "纸质表格审核签字 + 系统录入"}
  ],

  "kpi": [
    "调度指令零失误率",
    "异常告警平均响应时间 < 3 分钟",
    "月度可靠性指标 ≥ 99.9%"
  ],

  "collaboration": {
    "demand_source": "电网运行规则、负荷预测",
    "deliverables": "调度指令、操作票",
    "downstream_flow": "下发给变电站和电厂执行"
  },

  "business_systems": [
    {"system": "EMS 调度自动化", "scenario": "实时监盘与潮流计算"},
    {"system": "DTS 仿真系统", "scenario": "操作票预演验证"}
  ],

  "ai_assist_systems": [
    {"system": "南瑞预警平台", "scenario": "异常告警初筛"}
  ],

  "fault_scenarios": [
    {"scenario": "线路跳闸", "handling": "查报警 → 通知运维 → 调整潮流 → 出操作票"},
    {"scenario": "极端天气过载", "handling": "提前限电 + 启用备用机组"}
  ],

  "experience_goals": [
    {"title": "减少异常误报", "detail": "异常告警准确性提升,减少误报"},
    {"title": "集中查看关键数据", "detail": "多套系统数据能在一处看到"}
  ],

  "pain_points": [
    {"title": "误报多", "detail": "日均 200+ 告警里只有 5 条真正需要处理"},
    {"title": "系统切换", "detail": "排查一个问题要在 4 个系统间来回切"}
  ],

  "one_sentence_need": "[来源:{{SOURCE_FILE}}]: \"我们就希望系统能更聪明,别让我们当人肉过滤器\"",

  "representative_quotes": [
    "[来源:{{SOURCE_FILE}}]: \"原话1\"",
    "[来源:{{SOURCE_FILE}}]: \"原话2\""
  ]
}
```

## 注意事项

- 如果对齐字段集里没有某个字段(例如用户排除了 `knowledge_background`),JSON 里**完全不要有这个 key**,不要填空对象
- 如果对齐字段集里有自定义字段(例如 `decision_style`),按字段名直接添加进 JSON,值的格式由你自由判断,但同样要带证据
- `name` 和 `description` 是必填,无论字段集如何

现在开始提取。下方是访谈逐字稿:

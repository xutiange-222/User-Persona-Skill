# 04 用户画像内容确认

本文件由 `render_checkpoint_md.py` 从 `04-personas.draft.json` 生成。不要手写或局部修改。

用户看到的固定顺序：

1. 研究上下文
2. 画像总览
3. 每个画像的边界与中文字段正文
4. 字段展示取舍
5. 合并规则与证据风险
6. 待确认事项

机器字段名、组件类型和证据映射保存在 JSON 中。中文 MD 的字段标题优先读取 `03-field-alignment.json.fields_display_names`。

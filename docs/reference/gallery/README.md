# 组件 Gallery

| 文件 | 内容 |
|------|------|
| `gallery.html` | golden_samples vs renderer 并排对比 |
| `gallery-2b.html` | toB 主题 24 组件逐项预览 |
| `gallery-2c.html` | toC 主题组件预览 |

重新生成：

```bash
python scripts/components/tests/build_gallery.py
```

从 skill 根目录打开 `docs/reference/gallery/gallery-2b.html` 即可（CSS 链到 `assets/templates/`）。
